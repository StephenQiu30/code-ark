services:
  redis:
    image: redis:latest
    container_name: redis-start-dev
    ports:
      - "6379:6379"
    # 去掉了 --requirepass，仅开启持久化
    command: redis-server --appendonly yes
    volumes:
      - ./data:/data