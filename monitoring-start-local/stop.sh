#!/bin/sh
# 监控面板停止脚本
set -eu

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "${SCRIPT_DIR}"

echo "---------------------------------------"
echo "  监控面板停止中..."
echo "---------------------------------------"
docker compose stop

echo "---------------------------------------"
echo "  监控面板已停止"
echo "---------------------------------------"
